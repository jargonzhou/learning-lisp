(import (tspl wfreq)
        (io simple)
        (rnrs))

; output to console
(frequence "codes/scheme/chez/tests/tspl/wfreq.txt")
; output to file with replacing
(frequence "codes/scheme/chez/tests/tspl/wfreq.txt" "wfreq.out")

(exit)
